{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local windows (main shell internals + the settings prompt). Remote-context permissions live in odysseus-remote.json.",
  "windows": ["main", "settings"],
  "permissions": ["core:default"]
}
